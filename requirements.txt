# 발표자료 검사·렌더링 의존성.
# 덱 자체는 의존성이 없다 — HTML과 로컬 자산만으로 열린다.
# 아래는 시각자료를 다시 렌더링하거나 테스트를 돌릴 때만 필요하다.
# manim 은 정확히 고정한다. 장면 테스트가 글리프 색과 배치를 단정하는데
# 그 값이 manim 버전마다 달라진다 — 0.21.0 에서는 swarm 경계 글리프에 흰색이
# 섞여 들어와 테스트가 깨졌다. 올릴 때는 테스트 기대값을 함께 확인할 것.
manim==0.19.2
python-pptx>=0.6.23  # PPTX 산출물 내보내기와 그 검사
matplotlib>=3.7      # 텔레메트리 차트 이미지
numpy>=1.26          # 위 둘의 공통 의존
