# 발표자료 검사·렌더링 의존성.
# 덱 자체는 의존성이 없다 — HTML과 로컬 자산만으로 열린다.
# 아래는 시각자료를 다시 렌더링하거나 테스트를 돌릴 때만 필요하다.
# manim 은 정확히 고정한다. 장면 테스트가 글리프 색·배치를 단정하므로
# 렌더러가 바뀌면 기대값도 함께 확인해야 한다.
# 폰트도 함께 필요하다 — 장면이 "Noto Sans CJK KR" 을 요청하고, 없으면
# 한글 글리프가 폴백되며 색 단정이 깨진다(CI 는 fonts-noto-cjk 를 설치한다).
manim==0.19.2
python-pptx>=0.6.23  # PPTX 산출물 내보내기와 그 검사
matplotlib>=3.7      # 텔레메트리 차트 이미지
numpy>=1.26          # 위 둘의 공통 의존
