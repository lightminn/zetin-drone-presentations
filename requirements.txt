# 발표자료 검사·렌더링 의존성.
# 덱 자체는 의존성이 없다 — HTML과 로컬 자산만으로 열린다.
# 아래는 시각자료를 다시 렌더링하거나 테스트를 돌릴 때만 필요하다.
manim>=0.18          # 개념 다이어그램·영상 렌더링과 그 장면 테스트
python-pptx>=0.6.23  # PPTX 산출물 내보내기와 그 검사
matplotlib>=3.7      # 텔레메트리 차트 이미지
numpy>=1.26          # 위 둘의 공통 의존
