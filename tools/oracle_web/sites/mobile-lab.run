#!/bin/sh
set -eu

release_root=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
exec /usr/bin/python3 "$release_root/backend/server.py" --host 127.0.0.1 \
  --port "${ZETIN_WEB_PORT:?ZETIN_WEB_PORT is required}" \
  --static-root "$release_root/public"
